/*************************
 Run Spec
 **************************/
describe('Application Run Tests', spec);

function spec() {

    beforeEach(function() {
        module('dsft');
        inject(function() {
        })
    });
}